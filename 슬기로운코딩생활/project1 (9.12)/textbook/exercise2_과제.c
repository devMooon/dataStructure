//컴퓨터공학전공 20200675 문서연
/*#include <stdio.h>

int main(void)
{
	int x, y, z;
	int max, mid, min;

	x = 50; y = 43; z = 45;

	//max 먼저 찾기
	if (x > y && x > z) {
		max = x;
		if (y > z) {
			mid = y;
			min = z;
		}
		else {
			min = y;
			mid = z;
		}
	}
	else if (y > x && y > z) {
		max = y;
		if (x > z) {
			mid = x;
			min = z;
		}
		else {
			min = x;
			mid = z;
		}
	}
	else {
		max = z;
		if (x > y) {
			mid = x;
			min = y;
		}
		else {
			min = x;
			mid = y;
		}
	}

	printf("%d, %d, %d\n", min, mid, max);

	return 0;
}*/